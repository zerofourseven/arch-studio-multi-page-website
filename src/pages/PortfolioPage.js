//do map with the 12 grid images.
/*
e.g. but not necessarily:
{
        ingredients.map(ingredient => (
          <SaladItem
            key={ingredient.name}
            image={ingredient.image}
            name={ingredient.name}
          />
        ))
      }
*/